<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.maptest"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".GeometryMapActivity"
            android:exported="false" />
        <activity
            android:name=".PolygonMapActivity"
            android:exported="false" />
        <activity
            android:name=".LocationMapActivity"
            android:exported="false" />
        <activity
            android:name=".LineMapActivity"
            android:exported="false" />
        <activity
            android:name=".MarkerMapActivity"
            android:exported="false" />
        <activity
            android:name=".AddMapActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.DayNight">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RouteActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".MapActivity" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".MapActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.maptest" />
    </application>

</manifest>